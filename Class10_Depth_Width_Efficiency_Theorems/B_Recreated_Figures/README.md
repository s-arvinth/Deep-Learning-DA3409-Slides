# Class 10 — recreated-figures variant

Sequel to Class 9. Class 9 settles what a network *can* represent and at
what rate; this deck settles how the parameter budget should be
**shaped**. There is no overlap: the sawtooth, Telgarsky,
Eldan–Shamir, Montúfar, Serra and the VC bound live here, not in
Class 9.

```bash
cd src && python3 build_all.py
cd ..  && xelatex main.tex && xelatex main.tex     # twice
```
