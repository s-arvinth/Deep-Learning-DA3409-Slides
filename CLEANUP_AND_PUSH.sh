#!/bin/bash
# Superseded. Use the sync helper one level up instead:
#
#   cd ~/Documents/TA-Duty
#   ./overleaf-sync.sh push "your message"
#
# It commits as you, rebases on Overleaf so web edits survive, and
# mirrors to GitHub if a "github" remote is configured.
echo "Use:  cd ~/Documents/TA-Duty && ./overleaf-sync.sh push \"your message\""
