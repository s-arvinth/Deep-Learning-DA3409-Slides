# Class 9 — recreated-figures variant

All seven figures are generated by `src/build_all.py`; there are no
textbook images. See `src/README.md` for the module map and
`../../_shared/FIGURE_SOURCES.md` for the conventions.

```bash
cd src && python3 build_all.py
cd ..  && xelatex main.tex && xelatex main.tex     # twice
```
