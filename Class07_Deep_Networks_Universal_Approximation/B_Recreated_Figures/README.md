# Class 7 — recreated-figures variant

Nine of the ten figures are generated by `src/build_all.py`; the tenth
(`figs/Bishop6_8.pdf`) is reproduced from Bishop & Bishop (2024) and
cited on the slide that uses it. See `src/README.md`.

```bash
cd src && python3 build_all.py
cd ..  && xelatex main.tex && xelatex main.tex     # twice
```
