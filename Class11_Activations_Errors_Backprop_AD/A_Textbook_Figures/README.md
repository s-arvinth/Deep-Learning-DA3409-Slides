# Class 11 — textbook-figures variant

Reproduced with attribution:

* **Bishop & Bishop (2024)** — Fig. 6.12 (activations), 6.17–6.19
  (mixture density networks and the inverse problem), 8.1 (the
  backpropagation diagram), 8.2 (numerical-differentiation error),
  8.4–8.5 (forward- and reverse-mode evaluation traces).
* **Prince (2023)** — Fig. 5.14 (heteroscedastic loss), 7.7 (exploding
  forward and backward passes), 7.8 (computational graph).
  CC BY-NC-ND, <https://udlbook.github.io/udlbook/>.

Three plots have no textbook counterpart — the `σ' ≤ 1/4` bound and its
product over depth, the dying-ReLU count, and the forward/reverse sweep
comparison. They are generated; source in `../B_Recreated_Figures/src/`.
